# Packages

Release packages are produced by `scripts/package_app.sh` as deterministic `.tar.gz` archives containing `synapp.app.json` and the compiled Wasm entrypoint.

Generated archives are release artifacts and are not committed to the catalog source tree.